{
  "arguments": {
    "key": {
      "type": "String"
    }
    , "port": {
      "type": "Integer"
    }
  },
  "sourceEndpoints": {
    "MS SQL Server": {
      "type": "SQL",
      "url": "$sqlserver://localhost:<port>/test",
      "username": "sa",
      "password": "T0p-secret"
    },
    "MySQL": {
      "type": "SQL",
      "url": "$mysql://localhost:<port>/test",
      "username": "user",
      "password": "T0p-secret"
    }
    ,
    "PostgreSQL": {
      "type": "SQL",
      "url": "$postgresql://localhost:<port>/test",
      "username": "postgres",
      "password": "T0p-secret"
    }
  }
  ,
  "source": {    
    "type": "SQL",
    "endpoint": "$<key>",
    "blockingProcessor": false,
    "rowsQueuedBeforeDiscard": 10,
    "streamingFetchSize": 9,
    "query": "select d.id, d.instant, l.value as ref, d.value from testData d join testRefData l on d.lookup = l.id where d.id < 20 order by d.id"
  }
  ,
  "processors": [
    {
      "type":"LIMIT",
      "limit":13
    }
  ]
  ,
  "destination": {
    "type": "Logger"
  }
}